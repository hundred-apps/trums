export type Inquiry = {
    unique_id: string | null,
    unique_code: string | null,
    date: number | null,
    reference: string
    reference_id: string,
    reference_view: string,
    status: string,
    priority: string,
    description: string | null,
}